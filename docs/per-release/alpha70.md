# alpha70 — release notes

_Compared against `alpha69`._

## File-level changes

- Added: 0
- Removed: 0
- Changed: 2

### Changed

- `OblivionRemastered/Content/Dev/ObvData/Data/MagicLoader/Oscuro's_Oblivion_Overhaul_ARMO.json`
- `OblivionRemastered/Content/Dev/ObvData/Data/Oscuro's_Oblivion_Overhaul.esp`

## ESP changes — `Oscuro's_Oblivion_Overhaul.esp`

### Leveled Item List (LVLI) — +0 -0 ~128

**Changed:**

- `LL0NPCArmorLightCuirass25` (FormID `032BD3`) — + entry: level 1 × 1 → `030DB8`; + entry: level 4 × 1 → `024766`; + entry: level 4 × 1 → `001C4A`; + entry: level 4 × 1 → `030DB8`; + entry: level 5 × 1 → `001C4A`; + entry: level 6 × 1 → `024766`; + entry: level 6 × 1 → `001C4A`; + entry: level 6 × 1 → `030DB8`; + entry: level 7 × 1 → `02319A`; + entry: level 8 × 1 → `02319A`; + entry: level 8 × 1 → `024766`; + entry: level 8 × 1 → `001C4A`; + entry: level 8 × 1 → `030DB8`; + entry: level 10 × 1 → `001278`; + entry: level 10 × 1 → `003DCB`; + entry: level 12 × 1 → `003DCA`; + entry: level 14 × 1 → `01C6D3`; + entry: level 15 × 1 → `01C6D3`; + entry: level 15 × 1 → `02319A`; + entry: level 15 × 1 → `001278`; + entry: level 15 × 1 → `001C4A`; + entry: level 16 × 1 → `003DC9`; + entry: level 18 × 1 → `000D85`; + entry: level 18 × 1 → `003DC8`; + entry: level 20 × 1 → `02C0FE`; + entry: level 20 × 1 → `03AB99`; + entry: level 22 × 1 → `01C6D3`; + entry: level 22 × 1 → `02319A`; + entry: level 22 × 1 → `001278`; + entry: level 22 × 1 → `001C4A`; + entry: level 22 × 1 → `003DC7`; + entry: level 22 × 1 → `03AB99`; + entry: level 24 × 1 → `02299C`; + entry: level 24 × 1 → `003DC6`; + entry: level 24 × 1 → `03AB9A`; + entry: level 27 × 1 → `02C0FE`; + entry: level 27 × 1 → `000D85`; + entry: level 28 × 1 → `003DC5`; + entry: level 30 × 1 → `036343`; + entry: level 30 × 1 → `003DC4`; - entry: level 1 × 1 → `033E8B`
- `LL0NPCArmorLightBoots25` (FormID `033E97`) — + entry: level 1 × 1 → `030DB9`; + entry: level 4 × 1 → `024767`; + entry: level 4 × 1 → `001C49`; + entry: level 4 × 1 → `030DB9`; + entry: level 6 × 1 → `024767`; + entry: level 6 × 1 → `001C49`; + entry: level 6 × 1 → `030DB9`; + entry: level 7 × 1 → `02319B`; + entry: level 8 × 1 → `02319B`; + entry: level 8 × 1 → `024767`; + entry: level 8 × 1 → `001C49`; + entry: level 8 × 1 → `030DB9`; + entry: level 10 × 1 → `001276`; + entry: level 10 × 1 → `003DC2`; + entry: level 12 × 1 → `003DC1`; + entry: level 14 × 1 → `01C6D4`; + entry: level 15 × 1 → `01C6D4`; + entry: level 15 × 1 → `02319B`; + entry: level 15 × 1 → `001276`; + entry: level 15 × 1 → `001C49`; + entry: level 16 × 1 → `003DC0`; + entry: level 18 × 1 → `000D84`; + entry: level 18 × 1 → `003DBF`; + entry: level 20 × 1 → `02C0FC`; + entry: level 22 × 1 → `01C6D4`; + entry: level 22 × 1 → `02319B`; + entry: level 22 × 1 → `024767`; + entry: level 22 × 1 → `001276`; + entry: level 22 × 1 → `001C49`; + entry: level 24 × 1 → `02299F`; + entry: level 24 × 1 → `003DBE`; + entry: level 27 × 1 → `02C0FC`; + entry: level 27 × 1 → `000D84`; + entry: level 30 × 1 → `036341`; + entry: level 30 × 1 → `003DBD`; - entry: level 1 × 1 → `033E96`
- `LL0NPCArmorLightBoots50` (FormID `033E98`) — + entry: level 1 × 1 → `030DB9`; + entry: level 4 × 1 → `024767`; + entry: level 4 × 1 → `001C49`; + entry: level 4 × 1 → `030DB9`; + entry: level 6 × 1 → `024767`; + entry: level 6 × 1 → `001C49`; + entry: level 6 × 1 → `030DB9`; + entry: level 7 × 1 → `02319B`; + entry: level 8 × 1 → `02319B`; + entry: level 8 × 1 → `024767`; + entry: level 8 × 1 → `001C49`; + entry: level 8 × 1 → `030DB9`; + entry: level 10 × 1 → `001276`; + entry: level 10 × 1 → `003DC2`; + entry: level 12 × 1 → `003DC1`; + entry: level 14 × 1 → `01C6D4`; + entry: level 15 × 1 → `01C6D4`; + entry: level 15 × 1 → `02319B`; + entry: level 15 × 1 → `001276`; + entry: level 15 × 1 → `001C49`; + entry: level 16 × 1 → `003DC0`; + entry: level 18 × 1 → `000D84`; + entry: level 18 × 1 → `003DBF`; + entry: level 20 × 1 → `02C0FC`; + entry: level 22 × 1 → `01C6D4`; + entry: level 22 × 1 → `02319B`; + entry: level 22 × 1 → `024767`; + entry: level 22 × 1 → `001276`; + entry: level 22 × 1 → `001C49`; + entry: level 24 × 1 → `02299F`; + entry: level 24 × 1 → `003DBE`; + entry: level 27 × 1 → `02C0FC`; + entry: level 27 × 1 → `000D84`; + entry: level 30 × 1 → `036341`; + entry: level 30 × 1 → `003DBD`; - entry: level 1 × 1 → `033E96`
- `LL0NPCArmorLightBoots75` (FormID `033E99`) — + entry: level 1 × 1 → `030DB9`; + entry: level 4 × 1 → `024767`; + entry: level 4 × 1 → `001C49`; + entry: level 4 × 1 → `030DB9`; + entry: level 6 × 1 → `024767`; + entry: level 6 × 1 → `001C49`; + entry: level 6 × 1 → `030DB9`; + entry: level 7 × 1 → `02319B`; + entry: level 8 × 1 → `02319B`; + entry: level 8 × 1 → `024767`; + entry: level 8 × 1 → `001C49`; + entry: level 8 × 1 → `030DB9`; + entry: level 10 × 1 → `001276`; + entry: level 10 × 1 → `003DC2`; + entry: level 12 × 1 → `003DC1`; + entry: level 14 × 1 → `01C6D4`; + entry: level 15 × 1 → `01C6D4`; + entry: level 15 × 1 → `02319B`; + entry: level 15 × 1 → `001276`; + entry: level 15 × 1 → `001C49`; + entry: level 16 × 1 → `003DC0`; + entry: level 18 × 1 → `000D84`; + entry: level 18 × 1 → `003DBF`; + entry: level 20 × 1 → `02C0FC`; + entry: level 22 × 1 → `01C6D4`; + entry: level 22 × 1 → `02319B`; + entry: level 22 × 1 → `024767`; + entry: level 22 × 1 → `001276`; + entry: level 22 × 1 → `001C49`; + entry: level 24 × 1 → `02299F`; + entry: level 24 × 1 → `003DBE`; + entry: level 27 × 1 → `02C0FC`; + entry: level 27 × 1 → `000D84`; + entry: level 30 × 1 → `036341`; + entry: level 30 × 1 → `003DBD`; - entry: level 1 × 1 → `033E96`
- `LL0NPCArmorLightGauntlets25` (FormID `033E9B`) — + entry: level 1 × 1 → `030DBA`; + entry: level 4 × 1 → `001C4B`; + entry: level 4 × 1 → `030DBA`; + entry: level 5 × 1 → `001C4B`; + entry: level 6 × 1 → `001C4B`; + entry: level 6 × 1 → `030DBA`; + entry: level 8 × 1 → `001C4B`; + entry: level 8 × 1 → `030DBA`; + entry: level 10 × 1 → `00175F`; + entry: level 15 × 1 → `00175F`; + entry: level 15 × 1 → `001C4B`; + entry: level 18 × 1 → `000D86`; + entry: level 22 × 1 → `00175F`; + entry: level 22 × 1 → `001C4B`; + entry: level 27 × 1 → `000D86`; - entry: level 1 × 1 → `024765`
- `LL0NPCArmorLightGauntlets50` (FormID `033E9C`) — + entry: level 1 × 1 → `030DBA`; + entry: level 4 × 1 → `001C4B`; + entry: level 4 × 1 → `003DCD`; + entry: level 4 × 1 → `030DBA`; + entry: level 4 × 1 → `03BE6D`; + entry: level 5 × 1 → `001C4B`; + entry: level 6 × 1 → `001C4B`; + entry: level 6 × 1 → `003DCD`; + entry: level 6 × 1 → `030DBA`; + entry: level 7 × 1 → `023199`; + entry: level 8 × 1 → `023199`; + entry: level 8 × 1 → `001C4B`; + entry: level 8 × 1 → `003DCD`; + entry: level 8 × 1 → `030DBA`; + entry: level 8 × 1 → `03BE6D`; + entry: level 10 × 1 → `00175F`; + entry: level 10 × 1 → `003DD3`; + entry: level 12 × 1 → `003DD2`; + entry: level 14 × 1 → `01C6D6`; + entry: level 15 × 1 → `01C6D6`; + entry: level 15 × 1 → `023199`; + entry: level 15 × 1 → `00175F`; + entry: level 15 × 1 → `001C4B`; + entry: level 16 × 1 → `003DD1`; + entry: level 18 × 1 → `000D86`; + entry: level 18 × 1 → `003DD0`; + entry: level 20 × 1 → `02C100`; + entry: level 22 × 1 → `01C6D6`; + entry: level 22 × 1 → `023199`; + entry: level 22 × 1 → `00175F`; + entry: level 22 × 1 → `001C4B`; + entry: level 24 × 1 → `02299E`; + entry: level 24 × 1 → `003DCF`; + entry: level 27 × 1 → `02299E`; + entry: level 27 × 1 → `02C100`; + entry: level 27 × 1 → `000D86`; + entry: level 30 × 1 → `036340`; + entry: level 30 × 1 → `003DCE`; - entry: level 1 × 1 → `033E8D`
- `LL0NPCArmorLightGauntlets75` (FormID `033E9D`) — + entry: level 1 × 1 → `030DBA`; + entry: level 4 × 1 → `001C4B`; + entry: level 4 × 1 → `003DCD`; + entry: level 4 × 1 → `030DBA`; + entry: level 4 × 1 → `03BE6D`; + entry: level 5 × 1 → `001C4B`; + entry: level 6 × 1 → `001C4B`; + entry: level 6 × 1 → `003DCD`; + entry: level 6 × 1 → `030DBA`; + entry: level 7 × 1 → `023199`; + entry: level 8 × 1 → `023199`; + entry: level 8 × 1 → `001C4B`; + entry: level 8 × 1 → `003DCD`; + entry: level 8 × 1 → `030DBA`; + entry: level 8 × 1 → `03BE6D`; + entry: level 10 × 1 → `00175F`; + entry: level 10 × 1 → `003DD3`; + entry: level 12 × 1 → `003DD2`; + entry: level 14 × 1 → `01C6D6`; + entry: level 15 × 1 → `01C6D6`; + entry: level 15 × 1 → `023199`; + entry: level 15 × 1 → `00175F`; + entry: level 15 × 1 → `001C4B`; + entry: level 16 × 1 → `003DD1`; + entry: level 18 × 1 → `000D86`; + entry: level 18 × 1 → `003DD0`; + entry: level 20 × 1 → `02C100`; + entry: level 22 × 1 → `01C6D6`; + entry: level 22 × 1 → `023199`; + entry: level 22 × 1 → `00175F`; + entry: level 22 × 1 → `001C4B`; + entry: level 24 × 1 → `02299E`; + entry: level 24 × 1 → `003DCF`; + entry: level 27 × 1 → `02299E`; + entry: level 27 × 1 → `02C100`; + entry: level 27 × 1 → `000D86`; + entry: level 30 × 1 → `036340`; + entry: level 30 × 1 → `003DCE`; - entry: level 1 × 1 → `033E8D`
- `LL0NPCArmorLightGreaves25` (FormID `033E9E`) — + entry: level 1 × 1 → `030DBB`; + entry: level 4 × 1 → `024764`; + entry: level 4 × 1 → `001C4C`; + entry: level 4 × 1 → `030DBB`; + entry: level 5 × 1 → `001C4C`; + entry: level 6 × 1 → `024764`; + entry: level 6 × 1 → `001C4C`; + entry: level 6 × 1 → `030DBB`; + entry: level 7 × 1 → `023198`; + entry: level 8 × 1 → `023198`; + entry: level 8 × 1 → `024764`; + entry: level 8 × 1 → `001C4C`; + entry: level 8 × 1 → `030DBB`; + entry: level 10 × 1 → `001760`; + entry: level 10 × 1 → `003DDA`; + entry: level 12 × 1 → `003DD9`; + entry: level 14 × 1 → `01C6D5`; + entry: level 15 × 1 → `01C6D5`; + entry: level 15 × 1 → `023198`; + entry: level 15 × 1 → `001760`; + entry: level 15 × 1 → `001C4C`; + entry: level 16 × 1 → `003DD8`; + entry: level 18 × 1 → `000D87`; + entry: level 18 × 1 → `003DD7`; + entry: level 20 × 1 → `02C102`; + entry: level 22 × 1 → `01C6D5`; + entry: level 22 × 1 → `023198`; + entry: level 22 × 1 → `001760`; + entry: level 22 × 1 → `001C4C`; + entry: level 24 × 1 → `02299D`; + entry: level 24 × 1 → `003DD6`; + entry: level 27 × 1 → `02C102`; + entry: level 27 × 1 → `000D87`; + entry: level 30 × 1 → `036342`; + entry: level 30 × 1 → `003DD5`; - entry: level 1 × 1 → `033E94`
- `LL0NPCArmorLightGreaves50` (FormID `033E9F`) — + entry: level 1 × 1 → `030DBB`; + entry: level 4 × 1 → `024764`; + entry: level 4 × 1 → `001C4C`; + entry: level 4 × 1 → `030DBB`; + entry: level 5 × 1 → `001C4C`; + entry: level 6 × 1 → `024764`; + entry: level 6 × 1 → `001C4C`; + entry: level 6 × 1 → `030DBB`; + entry: level 7 × 1 → `023198`; + entry: level 8 × 1 → `023198`; + entry: level 8 × 1 → `024764`; + entry: level 8 × 1 → `001C4C`; + entry: level 8 × 1 → `030DBB`; + entry: level 10 × 1 → `001760`; + entry: level 10 × 1 → `003DDA`; + entry: level 12 × 1 → `003DD9`; + entry: level 14 × 1 → `01C6D5`; + entry: level 15 × 1 → `01C6D5`; + entry: level 15 × 1 → `023198`; + entry: level 15 × 1 → `001760`; + entry: level 15 × 1 → `001C4C`; + entry: level 16 × 1 → `003DD8`; + entry: level 18 × 1 → `000D87`; + entry: level 18 × 1 → `003DD7`; + entry: level 20 × 1 → `02C102`; + entry: level 22 × 1 → `01C6D5`; + entry: level 22 × 1 → `023198`; + entry: level 22 × 1 → `001760`; + entry: level 22 × 1 → `001C4C`; + entry: level 24 × 1 → `02299D`; + entry: level 24 × 1 → `003DD6`; + entry: level 27 × 1 → `02C102`; + entry: level 27 × 1 → `000D87`; + entry: level 30 × 1 → `036342`; + entry: level 30 × 1 → `003DD5`; - entry: level 1 × 1 → `033E94`
- `LL0NPCArmorLightGreaves75` (FormID `033EA4`) — + entry: level 1 × 1 → `030DBB`; + entry: level 4 × 1 → `024764`; + entry: level 4 × 1 → `001C4C`; + entry: level 4 × 1 → `030DBB`; + entry: level 5 × 1 → `001C4C`; + entry: level 6 × 1 → `024764`; + entry: level 6 × 1 → `001C4C`; + entry: level 6 × 1 → `030DBB`; + entry: level 7 × 1 → `023198`; + entry: level 8 × 1 → `023198`; + entry: level 8 × 1 → `024764`; + entry: level 8 × 1 → `001C4C`; + entry: level 8 × 1 → `030DBB`; + entry: level 10 × 1 → `001760`; + entry: level 10 × 1 → `003DDA`; + entry: level 12 × 1 → `003DD9`; + entry: level 14 × 1 → `01C6D5`; + entry: level 15 × 1 → `01C6D5`; + entry: level 15 × 1 → `023198`; + entry: level 15 × 1 → `001760`; + entry: level 15 × 1 → `001C4C`; + entry: level 16 × 1 → `003DD8`; + entry: level 18 × 1 → `000D87`; + entry: level 18 × 1 → `003DD7`; + entry: level 20 × 1 → `02C102`; + entry: level 22 × 1 → `01C6D5`; + entry: level 22 × 1 → `023198`; + entry: level 22 × 1 → `001760`; + entry: level 22 × 1 → `001C4C`; + entry: level 24 × 1 → `02299D`; + entry: level 24 × 1 → `003DD6`; + entry: level 27 × 1 → `02C102`; + entry: level 27 × 1 → `000D87`; + entry: level 30 × 1 → `036342`; + entry: level 30 × 1 → `003DD5`; - entry: level 1 × 1 → `033E94`
- `LL0NPCArmorLightHelmet25` (FormID `033EA5`) — + entry: level 1 × 1 → `030DBC`; + entry: level 4 × 1 → `024768`; + entry: level 4 × 1 → `030DBC`; + entry: level 5 × 1 → `001C4D`; + entry: level 6 × 1 → `024768`; + entry: level 6 × 1 → `001C4D`; + entry: level 6 × 1 → `030DBC`; + entry: level 7 × 1 → `02319C`; + entry: level 8 × 1 → `02319C`; + entry: level 8 × 1 → `024768`; + entry: level 8 × 1 → `001C4D`; + entry: level 8 × 1 → `030DBC`; + entry: level 10 × 1 → `001761`; + entry: level 10 × 1 → `003DE1`; + entry: level 12 × 1 → `003DE0`; + entry: level 14 × 1 → `01C6D7`; + entry: level 15 × 1 → `01C6D7`; + entry: level 15 × 1 → `02319C`; + entry: level 15 × 1 → `001761`; + entry: level 15 × 1 → `001C4D`; + entry: level 16 × 1 → `003DDF`; + entry: level 18 × 1 → `000D88`; + entry: level 18 × 1 → `003DDE`; + entry: level 20 × 1 → `02C104`; + entry: level 22 × 1 → `01C6D7`; + entry: level 22 × 1 → `02319C`; + entry: level 22 × 1 → `001761`; + entry: level 22 × 1 → `001C4D`; + entry: level 24 × 1 → `0229A1`; + entry: level 24 × 1 → `003DDD`; + entry: level 27 × 1 → `02C104`; + entry: level 27 × 1 → `000D88`; + entry: level 30 × 1 → `03633F`; + entry: level 30 × 1 → `003DDC`; - entry: level 1 × 1 → `033E8C`
- `LL0NPCArmorLightHelmet50` (FormID `033EA7`) — + entry: level 1 × 1 → `030DBC`; + entry: level 4 × 1 → `024768`; + entry: level 4 × 1 → `030DBC`; + entry: level 5 × 1 → `001C4D`; + entry: level 6 × 1 → `024768`; + entry: level 6 × 1 → `001C4D`; + entry: level 6 × 1 → `030DBC`; + entry: level 7 × 1 → `02319C`; + entry: level 8 × 1 → `02319C`; + entry: level 8 × 1 → `024768`; + entry: level 8 × 1 → `001C4D`; + entry: level 8 × 1 → `030DBC`; + entry: level 10 × 1 → `001761`; + entry: level 10 × 1 → `003DE1`; + entry: level 12 × 1 → `003DE0`; + entry: level 14 × 1 → `01C6D7`; + entry: level 15 × 1 → `01C6D7`; + entry: level 15 × 1 → `02319C`; + entry: level 15 × 1 → `001761`; + entry: level 15 × 1 → `001C4D`; + entry: level 16 × 1 → `003DDF`; + entry: level 18 × 1 → `000D88`; + entry: level 18 × 1 → `003DDE`; + entry: level 20 × 1 → `02C104`; + entry: level 22 × 1 → `01C6D7`; + entry: level 22 × 1 → `02319C`; + entry: level 22 × 1 → `001761`; + entry: level 22 × 1 → `001C4D`; + entry: level 24 × 1 → `0229A1`; + entry: level 24 × 1 → `003DDD`; + entry: level 27 × 1 → `02C104`; + entry: level 27 × 1 → `000D88`; + entry: level 30 × 1 → `03633F`; + entry: level 30 × 1 → `003DDC`; - entry: level 1 × 1 → `033E8C`
- `LL0NPCArmorLightHelmet75` (FormID `033EA8`) — + entry: level 1 × 1 → `030DBC`; + entry: level 4 × 1 → `024768`; + entry: level 4 × 1 → `030DBC`; + entry: level 5 × 1 → `001C4D`; + entry: level 6 × 1 → `024768`; + entry: level 6 × 1 → `001C4D`; + entry: level 6 × 1 → `030DBC`; + entry: level 7 × 1 → `02319C`; + entry: level 8 × 1 → `02319C`; + entry: level 8 × 1 → `024768`; + entry: level 8 × 1 → `001C4D`; + entry: level 8 × 1 → `030DBC`; + entry: level 10 × 1 → `001761`; + entry: level 10 × 1 → `003DE1`; + entry: level 12 × 1 → `003DE0`; + entry: level 14 × 1 → `01C6D7`; + entry: level 15 × 1 → `01C6D7`; + entry: level 15 × 1 → `02319C`; + entry: level 15 × 1 → `001761`; + entry: level 15 × 1 → `001C4D`; + entry: level 16 × 1 → `003DDF`; + entry: level 18 × 1 → `000D88`; + entry: level 18 × 1 → `003DDE`; + entry: level 20 × 1 → `02C104`; + entry: level 22 × 1 → `01C6D7`; + entry: level 22 × 1 → `02319C`; + entry: level 22 × 1 → `001761`; + entry: level 22 × 1 → `001C4D`; + entry: level 24 × 1 → `0229A1`; + entry: level 24 × 1 → `003DDD`; + entry: level 27 × 1 → `02C104`; + entry: level 27 × 1 → `000D88`; + entry: level 30 × 1 → `03633F`; + entry: level 30 × 1 → `003DDC`; - entry: level 1 × 1 → `033E8C`
- `LL0NPCArmorHeavyGauntlet100` (FormID `033EAB`) — + entry: level 6 × 1 → `00DE2A`; + entry: level 8 × 1 → `00DE30`; + entry: level 10 × 1 → `021BC3`; + entry: level 15 × 1 → `00DE2A`; + entry: level 15 × 1 → `00DE30`; + entry: level 18 × 1 → `00DE30`; + entry: level 20 × 1 → `036346`; + entry: level 20 × 1 → `00DE30`; + entry: level 27 × 1 → `00DE30`; - entry: level 35 × 1 → `036358`
- `LL0NPCArmorHeavyGreaves100` (FormID `033EAC`) — + entry: level 1 × 1 → `001C52`; + entry: level 4 × 1 → `021BC4`; + entry: level 6 × 1 → `00DE2B`; + entry: level 6 × 1 → `021BC4`; + entry: level 8 × 1 → `00DE31`; + entry: level 8 × 1 → `021BC4`; + entry: level 10 × 1 → `00DE31`; + entry: level 12 × 1 → `001205`; + entry: level 12 × 1 → `021BC4`; + entry: level 15 × 1 → `00DE2B`; + entry: level 15 × 1 → `00DE31`; + entry: level 18 × 1 → `00DE31`; + entry: level 20 × 1 → `00DE31`; + entry: level 22 × 1 → `001205`; + entry: level 27 × 1 → `001205`; + entry: level 27 × 1 → `00DE31`; + entry: level 27 × 1 → `021BC4`; - entry: level 1 × 1 → `01C6D0`; - entry: level 35 × 1 → `03635A`
- `LL0NPCArmorHeavyHelmet100` (FormID `033EAD`) — + entry: level 6 × 1 → `00DE2C`; + entry: level 8 × 1 → `00DE32`; + entry: level 12 × 1 → `001210`; + entry: level 15 × 1 → `00DE32`; + entry: level 18 × 1 → `001210`; + entry: level 18 × 1 → `0025F4`; + entry: level 18 × 1 → `00DE32`; + entry: level 22 × 1 → `03634B`; + entry: level 25 × 1 → `03634B`; + entry: level 27 × 1 → `00DE32`; - entry: level 35 × 1 → `036357`
- `LL0NPCArmorHeavyGreaves75` (FormID `033EB4`) — + entry: level 1 × 1 → `001C52`; + entry: level 4 × 1 → `01C6D0`; + entry: level 4 × 1 → `021BC4`; + entry: level 6 × 1 → `01C6D0`; + entry: level 6 × 1 → `0229A3`; + entry: level 6 × 1 → `00DE2B`; + entry: level 6 × 1 → `021BC4`; + entry: level 8 × 1 → `01C6D0`; + entry: level 8 × 1 → `0229A3`; + entry: level 8 × 1 → `00DE31`; + entry: level 8 × 1 → `021BC4`; + entry: level 10 × 1 → `0229A3`; + entry: level 10 × 1 → `003288`; + entry: level 10 × 1 → `00DE31`; + entry: level 12 × 1 → `0229A3`; + entry: level 12 × 1 → `001205`; + entry: level 12 × 1 → `003287`; + entry: level 12 × 1 → `021BC4`; + entry: level 15 × 1 → `036348`; + entry: level 15 × 1 → `00DE2B`; + entry: level 15 × 1 → `00DE31`; + entry: level 16 × 1 → `0229A3`; + entry: level 16 × 1 → `003286`; + entry: level 18 × 1 → `036348`; + entry: level 18 × 1 → `003285`; + entry: level 18 × 1 → `00DE31`; + entry: level 20 × 1 → `0229A3`; + entry: level 20 × 1 → `036348`; + entry: level 20 × 1 → `03634E`; + entry: level 20 × 1 → `00DE31`; + entry: level 22 × 1 → `036348`; + entry: level 22 × 1 → `001205`; + entry: level 24 × 1 → `003284`; + entry: level 27 × 1 → `0229A3`; + entry: level 27 × 1 → `036348`; + entry: level 27 × 1 → `03634E`; + entry: level 27 × 1 → `001205`; + entry: level 27 × 1 → `00DE31`; + entry: level 27 × 1 → `021BC4`; + entry: level 29 × 1 → `036354`; + entry: level 29 × 1 → `003283`; + entry: level 32 × 1 → `00CFD1`; + entry: level 34 × 1 → `036354`; + entry: level 35 × 1 → `003282`; - entry: level 1 × 1 → `033EAC`
- `LL0NPCArmorHeavyGreaves50` (FormID `033EB6`) — + entry: level 6 × 1 → `00DE2B`; + entry: level 8 × 1 → `00DE31`; + entry: level 10 × 1 → `00DE31`; + entry: level 12 × 1 → `0229A3`; + entry: level 15 × 1 → `00DE2B`; + entry: level 15 × 1 → `00DE31`; + entry: level 18 × 1 → `00DE31`; + entry: level 20 × 1 → `036348`; + entry: level 20 × 1 → `00DE31`; + entry: level 22 × 1 → `001205`; + entry: level 27 × 1 → `00DE31`; - entry: level 17 × 1 → `002AF0`; - entry: level 20 × 1 → `001205`; - entry: level 20 × 1 → `021BC4`; - entry: level 24 × 1 → `00CFD1`; - entry: level 25 × 1 → `002AF0`; - entry: level 27 × 1 → `002AF0`; - entry: level 35 × 1 → `03635A`
- `LL0NPCArmorHeavyGreaves25` (FormID `033EB7`) — + entry: level 1 × 1 → `001C52`; + entry: level 4 × 1 → `01C6D0`; + entry: level 4 × 1 → `021BC4`; + entry: level 6 × 1 → `01C6D0`; + entry: level 6 × 1 → `0229A3`; + entry: level 6 × 1 → `00DE2B`; + entry: level 6 × 1 → `021BC4`; + entry: level 8 × 1 → `01C6D0`; + entry: level 8 × 1 → `0229A3`; + entry: level 8 × 1 → `00DE31`; + entry: level 8 × 1 → `021BC4`; + entry: level 10 × 1 → `0229A3`; + entry: level 10 × 1 → `003288`; + entry: level 10 × 1 → `00DE31`; + entry: level 12 × 1 → `0229A3`; + entry: level 12 × 1 → `001205`; + entry: level 12 × 1 → `003287`; + entry: level 12 × 1 → `021BC4`; + entry: level 15 × 1 → `036348`; + entry: level 15 × 1 → `00DE2B`; + entry: level 15 × 1 → `00DE31`; + entry: level 16 × 1 → `0229A3`; + entry: level 16 × 1 → `003286`; + entry: level 18 × 1 → `036348`; + entry: level 18 × 1 → `003285`; + entry: level 18 × 1 → `00DE31`; + entry: level 20 × 1 → `0229A3`; + entry: level 20 × 1 → `036348`; + entry: level 20 × 1 → `03634E`; + entry: level 20 × 1 → `00DE31`; + entry: level 22 × 1 → `036348`; + entry: level 22 × 1 → `001205`; + entry: level 24 × 1 → `003284`; + entry: level 27 × 1 → `0229A3`; + entry: level 27 × 1 → `036348`; + entry: level 27 × 1 → `03634E`; + entry: level 27 × 1 → `001205`; + entry: level 27 × 1 → `00DE31`; + entry: level 27 × 1 → `021BC4`; + entry: level 29 × 1 → `036354`; + entry: level 29 × 1 → `003283`; + entry: level 32 × 1 → `00CFD1`; + entry: level 34 × 1 → `036354`; + entry: level 35 × 1 → `003282`; - entry: level 1 × 1 → `033EAC`
- `LL0NPCArmorHeavyHelmet75` (FormID `033EB8`) — + entry: level 1 × 1 → `001C54`; + entry: level 4 × 1 → `01C6CE`; + entry: level 4 × 1 → `021BC5`; + entry: level 6 × 1 → `01C6CE`; + entry: level 6 × 1 → `0229A4`; + entry: level 6 × 1 → `00DE2C`; + entry: level 6 × 1 → `021BC5`; + entry: level 8 × 1 → `01C6CE`; + entry: level 8 × 1 → `0229A4`; + entry: level 8 × 1 → `00DE32`; + entry: level 8 × 1 → `021BC5`; + entry: level 10 × 1 → `0229A4`; + entry: level 10 × 1 → `003280`; + entry: level 12 × 1 → `001210`; + entry: level 12 × 1 → `00327F`; + entry: level 15 × 1 → `036345`; + entry: level 15 × 1 → `00DE32`; + entry: level 16 × 1 → `00327E`; + entry: level 17 × 1 → `0229A4`; + entry: level 18 × 1 → `036345`; + entry: level 18 × 1 → `001210`; + entry: level 18 × 1 → `00327D`; + entry: level 18 × 1 → `00DE32`; + entry: level 20 × 1 → `0229A4`; + entry: level 20 × 1 → `03634B`; + entry: level 22 × 1 → `036345`; + entry: level 24 × 1 → `00327C`; + entry: level 24 × 1 → `00CFD2`; + entry: level 25 × 1 → `03634B`; + entry: level 25 × 1 → `0025F4`; + entry: level 27 × 1 → `0229A4`; + entry: level 27 × 1 → `036345`; + entry: level 27 × 1 → `03634B`; + entry: level 27 × 1 → `001210`; + entry: level 27 × 1 → `0025F4`; + entry: level 27 × 1 → `00DE32`; + entry: level 27 × 1 → `021BC5`; + entry: level 29 × 1 → `036351`; + entry: level 29 × 1 → `00327B`; + entry: level 32 × 1 → `00CFD2`; + entry: level 34 × 1 → `036351`; + entry: level 35 × 1 → `036357`; + entry: level 35 × 1 → `00327A`; - entry: level 1 × 1 → `033EAD`

_…108 more changed omitted (see JSON for full list)_

## ESP changes — `OOO_DeluxeEdition.esp`

_No record-level changes detected._

## ESP changes — `OOO_OOMC_Compatibility_Patch.esp`

_No record-level changes detected._

## ESP changes — `OOO_UnlimitedRingsReduxPatch.esp`

_No record-level changes detected._

## ESP changes — `OOO_UORP.esp`

_No record-level changes detected._

