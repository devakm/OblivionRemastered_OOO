# alpha52 — release notes

_Compared against `alpha51`._

## File-level changes

- Added: 0
- Removed: 0
- Changed: 1

### Changed

- `OblivionRemastered/Content/Dev/ObvData/Data/Oscuro's_Oblivion_Overhaul.esp`

## ESP changes — `Oscuro's_Oblivion_Overhaul.esp`

### Leveled Item List (LVLI) — +0 -0 ~79

**Changed:**

- `LL0NPCArmorHeavyBootsLvl100` (FormID `033EAA`) — + entry: level 25 × 1 → `00CFCE`
- `LL0NPCArmorHeavyGauntlet100` (FormID `033EAB`) — + entry: level 1 × 1 → `001C51`; + entry: level 4 × 1 → `021BC3`; + entry: level 6 × 1 → `021BC3`; + entry: level 8 × 1 → `021BC3`; + entry: level 12 × 1 → `001204`; + entry: level 14 × 1 → `021BC3`; + entry: level 17 × 1 → `002AEF`; + entry: level 22 × 1 → `001204`; + entry: level 24 × 1 → `00CFD0`; + entry: level 25 × 1 → `002AEF`; + entry: level 27 × 1 → `001204`; + entry: level 27 × 1 → `002AEF`; + entry: level 27 × 1 → `021BC3`; + entry: level 32 × 1 → `00CFD0`; - entry: level 1 × 1 → `01C6D2`
- `LL0NPCArmorHeavyBoots100` (FormID `033EBB`) — + entry: level 1 × 1 → `001C4F`; + entry: level 4 × 1 → `021BC1`; + entry: level 6 × 1 → `021BC1`; + entry: level 8 × 1 → `021BC1`; + entry: level 12 × 1 → `001202`; + entry: level 16 × 1 → `021BC1`; + entry: level 17 × 1 → `002AEE`; + entry: level 19 × 1 → `001202`; + entry: level 24 × 1 → `00CFCE`; + entry: level 25 × 1 → `002AEE`; + entry: level 27 × 1 → `001202`; + entry: level 27 × 1 → `002AEE`; + entry: level 27 × 1 → `021BC1`; + entry: level 32 × 1 → `00CFCE`; - entry: level 1 × 1 → `01C6CF`; - entry: level 17 × 1 → `0229A5`; - entry: level 19 × 1 → `036347`; - entry: level 24 × 1 → `03634D`
- `LL0NPCArmorHeavyCuirassLvl100` (FormID `033EC0`) — + entry: level 25 × 1 → `00CFCF`
- `LL0NPCArmorHeavyCuirass100iron` (FormID `001F14`) — + entry: level 1 × 1 → `001C50`; + entry: level 1 × 1 → `03AB9B`; + entry: level 1 × 1 → `03AB9F`
- `LL0NPCArmorHeavyCuirass100steel` (FormID `001F15`) — + entry: level 1 × 1 → `021BC2`; + entry: level 1 × 1 → `03AB9D`; + entry: level 1 × 1 → `03ABA1`
- `LL0NPCArmorHeavyCuirass100dwarven` (FormID `001F16`) — + entry: level 1 × 1 → `0011EB`; + entry: level 1 × 1 → `03AB97`; + entry: level 1 × 1 → `03AB9E`
- `LL0NPCArmorHeavyCuirass100orcish` (FormID `001F17`) — + entry: level 1 × 1 → `00210B`; + entry: level 1 × 1 → `03AB9C`; + entry: level 1 × 1 → `03ABA0`
- `LL0NPCArmorHeavyHelmet100akmref` (FormID `003281`) — + entry: level 4 × 1 → `021BC5`; + entry: level 6 × 1 → `021BC5`; + entry: level 8 × 1 → `021BC5`; + entry: level 10 × 1 → `001210`; + entry: level 10 × 1 → `021BC5`; + entry: level 12 × 1 → `001210`; + entry: level 12 × 1 → `021BC5`; + entry: level 16 × 1 → `001210`; + entry: level 16 × 1 → `0025F4`; + entry: level 16 × 1 → `021BC5`; + entry: level 18 × 1 → `001210`; + entry: level 18 × 1 → `0025F4`; + entry: level 18 × 1 → `021BC5`; + entry: level 24 × 1 → `001210`; + entry: level 24 × 1 → `0025F4`; + entry: level 24 × 1 → `00CFD2`; + entry: level 24 × 1 → `021BC5`; + entry: level 29 × 1 → `001210`; + entry: level 29 × 1 → `0025F4`; + entry: level 29 × 1 → `00CFD2`; + entry: level 29 × 1 → `021BC5`; + entry: level 35 × 1 → `001210`; + entry: level 35 × 1 → `0025F4`; + entry: level 35 × 1 → `00CFD2`; + entry: level 35 × 1 → `021BC5`; - entry: level 4 × 1 → `0229A4`; - entry: level 16 × 1 → `03634B`
- `LL0NPCArmorHeavyGauntlet100at35` (FormID `003DA5`) — + entry: level 1 × 1 → `001204`; + entry: level 1 × 1 → `002AEF`; + entry: level 1 × 1 → `00CFD0`; + entry: level 1 × 1 → `021BC3`
- `LL0NPCArmorHeavyGauntlet100at29` (FormID `003DA6`) — + entry: level 1 × 1 → `001204`; + entry: level 1 × 1 → `002AEF`; + entry: level 1 × 1 → `00CFD0`; + entry: level 1 × 1 → `021BC3`
- `LL0NPCArmorHeavyGauntlet100at24` (FormID `003DA7`) — + entry: level 1 × 1 → `001204`; + entry: level 1 × 1 → `002AEF`; + entry: level 1 × 1 → `00CFD0`; + entry: level 1 × 1 → `021BC3`
- `LL0NPCArmorHeavyGauntlet100at18` (FormID `003DA8`) — + entry: level 1 × 1 → `001204`; + entry: level 1 × 1 → `002AEF`; + entry: level 1 × 1 → `021BC3`
- `LL0NPCArmorHeavyGauntlet100at16` (FormID `003DA9`) — + entry: level 1 × 1 → `001204`; + entry: level 1 × 1 → `002AEF`; + entry: level 1 × 1 → `021BC3`
- `LL0NPCArmorHeavyGauntlet100at12` (FormID `003DAA`) — + entry: level 1 × 1 → `001204`; + entry: level 1 × 1 → `021BC3`
- `LL0NPCArmorHeavyGauntlet100at10` (FormID `003DAB`) — + entry: level 1 × 1 → `001204`; + entry: level 1 × 1 → `021BC3`
- `LL0NPCArmorHeavyGauntlet100akmref` (FormID `003DAC`) — + entry: level 4 × 1 → `021BC3`; + entry: level 6 × 1 → `021BC3`; + entry: level 8 × 1 → `021BC3`; + entry: level 10 × 1 → `001204`; + entry: level 10 × 1 → `021BC3`; + entry: level 12 × 1 → `001204`; + entry: level 12 × 1 → `021BC3`; + entry: level 16 × 1 → `001204`; + entry: level 16 × 1 → `002AEF`; + entry: level 16 × 1 → `021BC3`; + entry: level 18 × 1 → `001204`; + entry: level 18 × 1 → `002AEF`; + entry: level 18 × 1 → `021BC3`; + entry: level 24 × 1 → `001204`; + entry: level 24 × 1 → `002AEF`; + entry: level 24 × 1 → `00CFD0`; + entry: level 24 × 1 → `021BC3`; + entry: level 29 × 1 → `01C6D2`; + entry: level 29 × 1 → `001204`; + entry: level 29 × 1 → `002AEF`; + entry: level 29 × 1 → `021BC3`; + entry: level 35 × 1 → `001204`; + entry: level 35 × 1 → `002AEF`; + entry: level 35 × 1 → `00CFD0`; + entry: level 35 × 1 → `021BC3`
- `LL0NPCArmorHeavyBoots100at35` (FormID `003DAD`) — + entry: level 1 × 1 → `001202`; + entry: level 1 × 1 → `002AEE`; + entry: level 1 × 1 → `00CFCE`; + entry: level 1 × 1 → `021BC1`
- `LL0NPCArmorHeavyBoots100at29` (FormID `003DAE`) — + entry: level 1 × 1 → `001202`; + entry: level 1 × 1 → `002AEE`; + entry: level 1 × 1 → `00CFCE`; + entry: level 1 × 1 → `021BC1`
- `LL0NPCArmorHeavyBoots100at24` (FormID `003DAF`) — + entry: level 1 × 1 → `001202`; + entry: level 1 × 1 → `002AEE`; + entry: level 1 × 1 → `00CFCE`; + entry: level 1 × 1 → `021BC1`

_…59 more changed omitted (see JSON for full list)_

